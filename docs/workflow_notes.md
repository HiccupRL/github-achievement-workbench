# Workflow Notes

This repository is a public sandbox for lightweight GitHub workflow checks.
It records non-sensitive examples of issue, pull request, and repository
maintenance flows that can be reused when setting up future projects.

## Baseline flow

1. Keep changes scoped to one branch.
2. Open a pull request with a short summary and validation notes.
3. Merge only after the branch represents a real repository improvement.
